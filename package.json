{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm build:wasm && vite",
    "build": "pnpm build:wasm && tsc && vite build",
    "build:wasm": "cd wasm && emcmake cmake -B build && cmake --build build --target ${1:-main}",
    "build:wasm:debug": "cd wasm && emcmake cmake -B build && cmake --build build --target debug",
    "serve:wasm": "node scripts/serve-wasm.js",
    "test": "pnpm build:wasm && vitest",
    "test:ui": "pnpm build:wasm && vitest --ui",
    "test:run": "pnpm build:wasm && vitest run",
    "preview": "vite preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.7.0",
    "@vitest/ui": "^3.2.4",
    "concurrently": "^9.2.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}